package org.example.internship_system.entity.enums;

/**
 * Whether an internship offer is still accepting applications.
 * A company can CLOSE an offer; an admin can deactivate one.
 */
public enum OfferStatus {
    ACTIVE,
    CLOSED
}
