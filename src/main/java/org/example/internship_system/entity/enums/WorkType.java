package org.example.internship_system.entity.enums;

/**
 * Where the internship is performed. Students can filter offers by work type.
 */
public enum WorkType {
    ON_SITE,
    REMOTE,
    HYBRID
}
